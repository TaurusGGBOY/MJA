# 剑之川每日开始任务：人工指挥观察记录

> 用途：记录人工逐步指挥得到的真实界面流程，后续据此实现每日开始任务循环。

## 执行约束

- 所有模拟器操作使用 ADB。
- 未收到明确指令前，不自动点击、不自行推断下一步、不修改流程设计。
- 每个明确指令都记录：操作前状态、ADB 动作、操作后状态、截图/日志证据、等待条件、异常和结果。
- 只有经过人工指挥确认的步骤，才作为后续 workflow 的实现依据。

## 观察 001：食物增益替换提示

- 来源运行目录：`install/debug/runs/daily/2026-07-29T22:33:34.886474+08:00/`
- 已完成的前置动作：打开功能面板 → 打开背包 → 打开消耗品 → 定位龙井虾仁 → 点击使用一次。
- 弹窗文字：`当前已存在检定类食物的增益。使用道具将替换此效果，是否继续？`
- 按钮：`取消`、`确认`。
- 屏幕尺寸：`1280x720`。
- 历史截图中“确认”按钮大致中心：`(878, 503)`；再次执行时必须先用 ADB 截图确认弹窗仍在，不能盲点。
- 历史截图证据：`after.png`。
- 当前实时状态：最近一次 ADB 截图已不再显示该弹窗，处于游戏地图页；因此没有执行无条件点击。

## 待人工指挥

## 观察 002：连续食用龙井虾仁

- 初始数量：`39`。
- 已确认成功食用：`5` 个；数量变化为 `39 → 38 → 37 → 36 → 35 → 34`。
- 每次成功路径：点击 `使用` → 出现增益替换提示 → ADB 点击 `确认` → 等待“恢复体力”完成 → 返回食物详情页。
- 第 6 次点击 `使用` 未出现替换确认弹窗，数量保持 `34`。
- 第 6 次页面提示：`你吃得太撑了，请手抚腹部转圈按摩，或者歇着……`。
- 当前页面：消耗品列表，龙井虾仁详情已选中，当前拥有 `34`。
- 当前阻塞：游戏要求处理“吃得太撑”状态；具体选择“按摩”或“歇着”尚未由人工确认。

## 观察 003：人工确认食物任务完成态

- 人工确认：前面已有两次食用结果未被运行记录正确计入；结合实际账号状态，龙井虾仁本次任务已经达到 `6/6`。
- 固定流程：进入消耗品 → 使用龙井虾仁 → 若出现增益替换提示则点击确认 → 等待回到消耗品列表 → 重复使用。
- 不再进入“侠客选择”分支；本账号真实路径没有该页面。
- 当游戏在顶部显示 `你吃得太撑了，请手抚腹部转圈按摩，或者歇着……` 时，视为食物任务成功完成。
- “吃得太撑” OCR 区域已按实机截图固定为 `[300, 0, 700, 80]`。
- 实机复跑验证：`EAT_STAMINA_FOOD_DAILY` 返回 `already_complete`，数量保持 `34`，没有再次消耗食物。
- 验证运行目录：`install/debug/runs/daily/2026-07-29T23:09:23.898615+08:00/`。

后续观察对象切换到任务顺序中的下一个：`DUNGEON_SWEEP_DAILY`（副本扫荡）。

## 观察 004：副本扫荡与装备满阻塞

- 进入副本后初始选中的是`贼寇山洞`；必须滚动列表并选择左侧底部的`燕王秘陵`。
- `燕王秘陵`页面标题位于屏幕中部，实机标题区域约为 `[300, 300, 700, 180]`；原顶部 OCR 区域会导致选择动作后的后置验证漏检。
- 扫荡面板右侧选择`大师 80级`，点击该行的`+`，本次可用副本票为 `2`，分配后显示 `2(-2)`。
- 点击`开始扫荡`后弹窗文字为：`是否进行以下副本的扫荡？燕王秘陵(大师)x2`；点击`确认`。
- 曾出现阻塞提示：`背包已满，请先进行装备分解后再进行扫荡`。该状态必须停止并等待人工分解装备，自动化不得自行分解。
- 人工分解装备后，重新选择`燕王秘陵`并完成同一流程；结果弹窗显示`恭喜获得`，副本票从 `2/10` 变为 `0/10`。
- 奖励弹窗通过点击空白处关闭，再点击副本页右上角关闭按钮返回主界面。
- 固化规则：识别`背包已满`作为 `dungeon_bag_full`，返回 `failed / DUNGEON_BAG_FULL` 等待人工处理；自动化绝不分解装备。正常奖励弹窗关闭后再退出副本，保证后续任务从主界面开始。
- 实机证据：
  - `install/debug/manual/dungeon-yanwang-selected.png`
  - `install/debug/manual/dungeon-sweep-assigned.png`
  - `install/debug/manual/dungeon-sweep-confirm.png`
  - `install/debug/manual/dungeon-sweep-result.png`

## 观察 005：采集部署 Android 后置条件

- 验证运行目录：`install/debug/runs/daily/2026-07-30T00:44:07.057214+08:00/`。
- Maa Android controller 实机结果：`already_complete`。
- 实际动作计数：打开画卷 → 选择偃武世界 → 打开采集部署。
- 原问题是 `collection.page` OCR 后置条件漏检；修复后没有停在入口页，已完成态可以正常收敛。
- 该结果是一次已完成/no-op 分支证据，尚不足以把正式验收记录改成 `live_verified`。

## 观察 006：日常奖励与战令 Android 验证

- 日常奖励第一次运行成功点击了一个日常奖励行，但停在正常的“恭喜获得”弹窗；旧 OCR 区域没有识别到底部“点击空白处关闭”。
- 按实机截图将奖励弹窗 OCR 区域扩展到 `[0, 120, 1280, 600]`，并加入底部“点击空白处关闭”识别。
- 第二次运行目录：`install/debug/runs/daily/2026-07-30T00:47:25.394554+08:00/`，结果为 `already_complete`。
- 战令运行目录：`install/debug/runs/daily/2026-07-30T00:48:23.844191+08:00/`，结果为 `completed`；实际完成任务奖励和基础版红点奖励领取，两个预期奖励弹窗均已关闭。
