# MFW 任务顺序说明会话

- `scope_mode`: `catalog`
- `candidate`: `install/mfw-manual-completion-20260814-r2`
- `source`: `install/mfw-manual-completion-20260814-r2/interface.json` 的 `import` 顺序
- `business_task_count`: 22
- `current_index`: 22
- `phase`: `offline_verified`
- `live_game_run`: 禁止
- `change_ledger`: 16 条改动已实施并完成离线校验；真实 MFW 验收待后续单任务执行

## 冻结任务顺序

1. `MAIL_REWARD_DAILY` — 领取邮件奖励
2. `SHOP_FREE_GIFT_DAILY` — 商城每日免费礼包
3. `BUY_TEA_DAILY` — 购买茶叶
4. `FREE_APPRAISAL_DAILY` — 免费鉴宝
5. `TRIAL_SWORD_DAILY` — 每日试剑
6. `HERO_DISPATCH_DAILY` — 侠客派遣
7. `COLLECTION_DEPLOYMENT_DAILY` — 采集收取
8. `WEEKLY_FREE_GIFT_MONDAY` — 周一免费礼包（周一限定；2026-08-14 不适用）
9. `SHADOW_RUINS_DAILY` — 蜃影武墟
10. `SPEND_CONDENSATE_DAILY` — 消耗凝晶
11. `MARTIAL_STUDY_BREAKTHROUGH_DAILY` — 武学研习突破
12. `EAT_STAMINA_FOOD_DAILY` — 食用体力食物
13. `EQUIPMENT_DECOMPOSE_DAILY` — 装备分解（背包整理）
14. `DUNGEON_SWEEP_DAILY` — 副本扫荡
15. `JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY` — 剑林凝晶体力
16. `RING_CHALLENGE_DAILY` — 擂台挑战
17. `BREAK_ARRAY_MARTIAL_DAILY` — 破阵演武（每日三次，活动结束，已停用）
18. `GUILD_ACTIVITY_CHALLENGE_DAILY` — 帮会活动征讨
19. `GUILD_AFFAIRS_DAILY` — 帮会事务
20. `GUILD_DONATION_DAILY` — 帮会捐献
21. `DAILY_TASK_REWARD_CLAIM_DAILY` — 日常任务奖励
22. `BATTLE_PASS_REWARD_DAILY` — 战令基础奖励

## 当前任务

`SHOP_FREE_GIFT_DAILY`（2/22）已说明且无改动；已自动推进到 `BUY_TEA_DAILY`（3/22）。

`BUY_TEA_DAILY`（3/22）已说明，改动要求和澄清回答已登记；已自动推进到 `FREE_APPRAISAL_DAILY`（4/22）。

`FREE_APPRAISAL_DAILY`（4/22）已说明，改动要求和澄清回答已登记；已自动推进到 `TRIAL_SWORD_DAILY`（5/22）。

`TRIAL_SWORD_DAILY`（5/22）已说明，改动要求和澄清回答已登记；已自动推进到 `HERO_DISPATCH_DAILY`（6/22）。

`HERO_DISPATCH_DAILY`（6/22）已说明且无新增改动；已自动推进到 `COLLECTION_DEPLOYMENT_DAILY`（7/22）。

`COLLECTION_DEPLOYMENT_DAILY`（7/22）已说明，改动要求和澄清回答已登记；已自动推进到 `WEEKLY_FREE_GIFT_MONDAY`（8/22）。

`WEEKLY_FREE_GIFT_MONDAY`（8/22）已说明，改动要求和澄清回答已登记；已自动推进到 `SHADOW_RUINS_DAILY`（9/22）。

`SHADOW_RUINS_DAILY`（9/22）已说明，改动要求和澄清回答已登记；已自动推进到 `SPEND_CONDENSATE_DAILY`（10/22）。

`SPEND_CONDENSATE_DAILY`（10/22）已说明且无新增改动；已自动推进到 `MARTIAL_STUDY_BREAKTHROUGH_DAILY`（11/22）。

`MARTIAL_STUDY_BREAKTHROUGH_DAILY`（11/22）已说明，改动要求和澄清回答已登记；已自动推进到 `EAT_STAMINA_FOOD_DAILY`（12/22）。

`EAT_STAMINA_FOOD_DAILY`（12/22）已说明，改动要求和澄清回答已登记；已自动推进到 `EQUIPMENT_DECOMPOSE_DAILY`（13/22）。

`EQUIPMENT_DECOMPOSE_DAILY`（13/22）已说明，改动要求和澄清回答已登记；已自动推进到 `DUNGEON_SWEEP_DAILY`（14/22）。

`DUNGEON_SWEEP_DAILY`（14/22）已说明，改动要求和澄清回答已登记；已自动推进到 `JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY`（15/22）。

`JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY`（15/22）已说明，改动要求和澄清回答已登记；已自动推进到 `RING_CHALLENGE_DAILY`（16/22）。

`RING_CHALLENGE_DAILY`（16/22）已说明，改动要求和澄清回答已登记；已自动推进到 `BREAK_ARRAY_MARTIAL_DAILY`（17/22）。

`BREAK_ARRAY_MARTIAL_DAILY`（17/22）已说明，改动要求和澄清回答已登记并标记停用；已自动推进到 `GUILD_ACTIVITY_CHALLENGE_DAILY`（18/22）。

`GUILD_ACTIVITY_CHALLENGE_DAILY`（18/22）已说明，改动要求和澄清回答已登记；已自动推进到 `GUILD_AFFAIRS_DAILY`（19/22）。

`GUILD_AFFAIRS_DAILY`（19/22）已说明，改动要求和澄清回答已登记；已自动推进到 `GUILD_DONATION_DAILY`（20/22）。

`GUILD_DONATION_DAILY`（20/22）已说明且无新增改动；已自动推进到 `DAILY_TASK_REWARD_CLAIM_DAILY`（21/22）。

`DAILY_TASK_REWARD_CLAIM_DAILY`（21/22）已说明且无新增改动；已自动推进到 `BATTLE_PASS_REWARD_DAILY`（22/22）。

`BATTLE_PASS_REWARD_DAILY`（22/22）已说明，用户确认没有新增改动；本轮任务目录阅读与变更收集完成。

## 实施结果（2026-08-14）

- 16 条变更已统一写入当前候选代码与资源，包含成功判定前移、删除冗余复验、蜃影武墟/剑林战前准备勾选、帮会事务滚动与完整回主页收尾等要求。
- 已停用 `BREAK_ARRAY_MARTIAL_DAILY`，保留任务目录和 Pipeline 以便历史追溯。
- 已完成离线校验：1722 个 Pipeline 节点资源有效；任务引用缺失数为 0；Python 编译、策略导入和 `git diff --check` 均通过。
- 现有契约测试仍有 2 个旧断言失败：一个要求已删除的副本票耗尽复验，另一个要求把战斗失败判为失败；这两项与本轮用户要求相反，未恢复旧逻辑。
- 本轮严格禁止启动真实游戏/MFW；真实验收仍需后续按 `GAME_START + 单个任务`、新鲜结果证据执行。

### 今日手动完成模式（2026-08-14）

- 因用户已手动完成全部业务任务，22 个任务入口统一先经过临时手动完成门；今天命中后不执行任何业务点击，直接写入 `already_complete`。
- 结果后置条件统一为 `manual.user_completed_today`，因此不会把人工完成伪装成自动执行成功。
- 该门只在 2026-08-14 生效，2026-08-15 自动失效；原有真实执行和 `success` 分支未删除，明天可使用新候选做单任务验收。
- 已生成并通过候选校验：`install/mfw-manual-completion-20260814-r2`；明天测试应使用该候选，不要继续使用已过期的 `mfw-shared-convergence-20260813-r3`。
- 本次仅完成资源、注册、结果写入和日期边界的离线验证，未启动真实游戏、ADB 或 MFW。

### 最终行为覆盖（以当前实现为准）

1. 邮件：删除空邮件两次判断；奖励弹窗出现即记录成功。
2. 茶叶：识别“500文”即可购买；购买结果弹窗出现即成功，不验库存。
3. 鉴宝：结果弹窗出现即成功，删除已使用/次数复验。
4. 试剑：直接领取；免费试剑奖励弹窗出现即成功，删除免费次数复验。
5. 采集：点击一键收获即成功，删除已收获、奖励和收获状态复验。
6. 周礼包：奖励弹窗出现即成功，删除领取状态及付费/价格分支。
7. 蜃影武墟：画卷进入后最多处理 3 次“探索中/可探索”；战前准备勾选后挑战；删除推荐、传送、独立开战、已击破等分支；战斗失败直接失败；领取最终奖励并回主页成功。
8. 武学研习：删除 Python 复杂状态机，仅保留成功卡领取/关闭和无成功卡收尾。
9. 体力食物：体力已满或累计使用 5 次成功，不验数量减少。
10. 装备分解：品质筛选为“乙级及以下”。
11. 副本：目标改为风雪神道；结果弹窗出现即成功，不验副本票耗尽。
12. 剑林：第二档高价体力直接退出并成功；体力低于 20 成功；删除错误滚动条件和“领取”完成判断；挑战前确保跳过战前准备已勾选。
13. 擂台：删除“论剑未开启”判断。
14. 破阵演武：活动结束，任务停用但保留目录。
15. 帮会活动：战斗胜利、失败均成功，删除结果后的 `0/2` 复验，完成完整回主页收尾。
16. 帮会事务：持续向下滚动至无更多事务；开始事务最多 6 次，完成后回主页。

下方“任务说明依据”保留本次修改前的原始审计快照；最终行为以当前 Pipeline、策略和任务选择配置为准。

## 第 12 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/EAT_STAMINA_FOOD_DAILY.json`
- 入口：`MJA_EAT_STAMINA_FOOD_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/eat_stamina_food_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `EAT_STAMINA_FOOD_DAILY_DEFINITION`；特殊决策逻辑位于 `agent/workflows/definitions/batch1.py`
- 进入路径：主页 → 资源入口 → 背包/资源页 → 食物分类 →“食物”标签 → 食物列表；启动时会尝试从剑林、功能面板、副本、食物页或背包残留页面恢复。
- 食物选择：最多检查 6 个候选，只处理识别为“龙井虾仁”且同时有物品名称、数量和“使用”按钮证据的卡片；数量 1 至 6 分别有对应使用分支。
- 使用与循环：点击使用后，若出现体力已满/吃得太撑则按已完成处理；若出现增益替换提示则确认替换；识别“正在恢复体力/恢复体力成功”后，还要进入后置探测并验证数量减少，再决定成功或继续食用，最多继续 5 次。
- 结束状态：验证使用成功后记录 `success`；没有安全的龙井虾仁卡片记录 `not_eligible`；体力已满记录 `already_complete`；随后关闭食物页并进入公共停止节点。
- 已知风险：Pipeline 明确要求使用后的数量减少验证，而状态机定义主要表达“使用结果后继续循环”，两者成功语义不完全一致；Pipeline 收尾关闭背包后没有单独的主页边界验证。
- 当前任务暂无新增改动账目。

## 第 13 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/EQUIPMENT_DECOMPOSE_DAILY.json`
- 入口：`MJA_EQUIPMENT_DECOMPOSE_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/equipment_decompose_daily.json`
- 状态机定义：未找到该任务对应的独立 Python 状态机定义；任务由 Pipeline 和策略配置驱动。
- 进入路径：主页 → 资源入口 → 装备/背包 → 分解页面；启动时会尝试从功能面板、资源页或装备页残留状态恢复。
- 分解筛选：打开品质筛选并选择“乙级及以下”，确认筛选已生效；再打开等级筛选并选择“80级及以下”，确认筛选已生效。
- 执行路径：点击“批量选择”→点击“确认分解”→在确认弹窗中点击最终“确认”。
- 成功与收尾：最终确认后，识别“分解成功/分解完成”或仍处于分解页/装备页，就记录 `success`；随后尝试关闭分解页并确认回到主页，再进入公共停止节点。
- 失败路径：恢复失败或任一必要页面、筛选、确认按钮无法识别时，记录 `EQUIPMENT_DECOMPOSE_POSTCONDITION_MISSING` 并中止。
- 安全边界：该任务标记为消耗型和有状态任务，最多 32 步；每个打开、筛选、批量选择、确认和关闭动作最多执行 1 次。当前没有显式的“无可分解装备/已完成”分支。
- 已知风险：最终确认后只要仍识别到分解页或装备页也会记成功，不一定要求识别到“分解成功”文字；同时没有独立 Python 状态机与 Pipeline 做第二套语义校验。
- 当前任务暂无新增改动账目。

## 第 14 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/DUNGEON_SWEEP_DAILY.json`
- 入口：`MJA_DUNGEON_SWEEP_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/dungeon_sweep_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `DUNGEON_SWEEP_DAILY_DEFINITION`；特殊恢复和资格判断位于 `agent/workflows/definitions/batch1.py`
- 进入路径：主页 → 副本 → 副本列表；最多滚动 4 次后选择“燕王秘陵”，再打开“大师 80级”扫荡面板。
- 资格判断：副本票显示为 0 时记为 `not_eligible`；扫荡功能未解锁时也记为 `not_eligible`；识别到背包已满时记录失败 `DUNGEON_BAG_FULL`，不会继续扫荡。
- 执行路径：确认目标副本、票据和扫荡条件后，逐步分配“副本票”，点击“开始扫荡”，在确认弹窗中再次确认，等待扫荡结果并关闭结果弹窗。
- 成功边界：Pipeline 关闭结果弹窗后，还要求回到副本页并识别副本票已耗尽，随后记录 `success`，再关闭副本页并返回主页。
- 状态机差异：Python 定义以结果弹窗关闭后回到副本页作为完成标记；Pipeline 额外要求票据耗尽，二者成功条件不完全一致。状态机还支持从已选中的燕王秘陵页或已打开的扫荡面板恢复。
- 安全边界：任务标记为消耗型和战斗型，最多 160 步；副本票资源上限为 20，分配票据最多 100 次，打开扫荡面板最多重试 2 次。
- 当前任务暂无新增改动账目。

## 第 11 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/MARTIAL_STUDY_BREAKTHROUGH_DAILY.json`
- 入口：`MJA_MARTIAL_STUDY_BREAKTHROUGH_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/martial_study_breakthrough_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `MARTIAL_STUDY_BREAKTHROUGH_DAILY_DEFINITION`
- 共同进入路径：主页 → 功能面板 → 武学研习页面；启动时会尝试从茶叶残留页面或既有研习页面恢复。
- Pipeline 明确路径：识别“研习成功”卡后点击领取，识别并关闭结果弹窗，返回研习页继续领取，最多领取 3 次；没有成功卡时关闭研习页，确认回到面板或主页，记录成功（后置条件为“成功卡已领取或没有成功卡”）。
- Python 状态机额外路径：识别满 3 个进行中槽位时按已完成结束；识别空槽时打开槽位，根据“研习/突破”按钮和材料充足证据执行研习、突破和确认；突破后关闭详情页；成功弹窗关闭后继续处理其他槽位；材料不足可在最多 9 次研习动作内寻找可用组合，无法确认材料或配置时 fail closed。
- 状态机成功/结束：完成后回到面板或主页记录完成；满槽位为 `already_complete`；无可用配置为 `not_eligible`；未知页面、材料证据不足或动作上限耗尽为失败。
- 已知风险：Pipeline 没有显式的空槽、研习、突破、材料和确认节点，而状态机定义包含这些节点；当前候选包的实际执行语义需要后续统一时明确采用并对齐。
- 当前任务暂无新增改动账目。

## 第 10 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/SPEND_CONDENSATE_DAILY.json`
- 入口：`MJA_SPEND_CONDENSATE_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/spend_condensate_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `SPEND_CONDENSATE_DAILY_DEFINITION`
- 正常路径：主页 → 画卷 → 偃武世界 → 偃武世界货币兑换；处理完后切换到云州 → 云州货币兑换。
- 启动恢复：如果残留在日常任务页、茶叶详情/商城、画卷、偃武世界或云州页面，Pipeline 先按识别结果清理或从对应页面恢复，再进入兑换流程。
- 偃武世界分支：识别购买页和长定壹钱；若识别 `0/12500`，关闭购买页并转云州；否则勾选最大数量，只有同时确认购买按钮、凝晶货币证据和 50,000 价格时才购买；识别奖励弹窗后关闭，并验证偃武世界为 `0/12500`。
- 云州分支：流程与偃武世界相同，兑换南北通宝；识别售罄则关闭购买页，购买成功后关闭奖励弹窗并验证云州为 `0/12500`。
- 成功边界：两地都达到 `0/12500` 后记录 `success`，两地原本都售罄则记录 `already_complete`；当前 Pipeline 随后直接进入 `MJA_COMMON_STOP`，没有单独的回主页验证节点。
- 消费保护：任务策略最多消耗 100,000 凝晶，每地最多 50,000；价格或货币证据不完整时记录 `CONDENSATE_PRICE_OR_CURRENCY_UNVERIFIED` 并失败，不执行购买。
- 当前任务暂无新增改动账目。

## 第 9 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/SHADOW_RUINS_DAILY.json`
- 入口：`MJA_SHADOW_RUINS_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/shadow_ruins_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `SHADOW_RUINS_DAILY_DEFINITION`
- 进入路径：主页 → 画卷 → 蜃影武墟；误进入普通副本页会记录 `SHADOW_WRONG_ENTRY_PAGE` 失败。
- 初始分支：识别“暂无发现新的武墟/已击破”时走已完成路径；识别“探索中/可探索”时选择当前武墟并点击“前往”。
- 探索分支：处理自动寻路确认；可选处理推荐阵容；在探索页按前景格推进、必要时打开传送并确认；识别战斗入口后点击“开战”。
- 战斗分支：等待战斗进行状态；识别胜利后关闭战斗结果并继续推进领取宝箱；识别失败则关闭失败弹窗后回到战斗入口；结果未知直接失败。
- 奖励与终局：关闭普通奖励/宝箱奖励弹窗；识别“首领已被击败”后点击确认，处理最终奖励；识别“已击破”或直接回到主页。
- 成功边界：重启游戏界面后确认回到主页，才记录 `success`，后置条件为 `shadow.no_active_or_done_and_home`；初始无可用武墟也经过同一主页边界后按已完成路径结束。
- 有限性：传送确认最多 8 次，前景推进最多 40 次，战斗最多 12 次，失败弹窗关闭最多 3 次，宝箱推进/领取路径最多 12 次；超限分别记录专用失败码并终止。
- 黑屏分支：检测到大面积纯黑时重启游戏界面，等待恢复后回主页或回到 `GAME_START`；恢复失败记录失败。
- 当前任务暂无新增改动账目。

## 第 8 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/WEEKLY_FREE_GIFT_MONDAY.json`
- 入口：`MJA_WEEKLY_FREE_GIFT_MONDAY_START`
- Pipeline：`resource/base/pipeline/daily/weekly_free_gift_monday.json`
- 状态机定义：`agent/workflows/definitions/batch1.py` 中的 `WEEKLY_FREE_GIFT_MONDAY_DEFINITION`
- 日期策略：`agent/workflows/catalog.py` 将该任务设置为 `monday_only=True`；运行引擎在非周一直接写入 `not_eligible / weekday_not_eligible`，不执行游戏导航。
- 周一执行路径：主页 → 功能面板 → 商城 → 礼包 →“每周必买/每周必购”。
- 已领取分支：识别“已领取/已购买/已完成”，记录 `already_complete`，再关闭周常商城页。
- 免费分支：识别“免费领取/免费”后点击领取；识别并关闭奖励弹窗；再识别“已领取/已购买/已完成”，记录 `success`。
- 非免费分支：仅识别到“每周特价”且没有免费或已领取标记时，记录 `not_eligible`；免费与付费同时出现、价格冲突或价格无法确认时 fail closed，记录失败。
- 收尾边界：Pipeline 的成功、已完成和不适用分支都进入 `MJA_WEEKLY_CLOSE` 关闭商城页后停止；状态机定义的目标后置条件是回到主页，但当前 Pipeline 没有单独的主页验证节点，存在边界证据不一致，待后续统一处理时核对。
- 当前任务暂无新增改动账目。

## 第 7 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/COLLECTION_DEPLOYMENT_DAILY.json`
- 入口：`MJA_COLLECTION_DEPLOYMENT_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/collection_deployment_daily.json`
- 状态机定义：`agent/workflows/definitions/batch1.py` 中的 `COLLECTION_DEPLOYMENT_DAILY_DEFINITION`
- 当前配置路径：主页 → 画卷 → 偃武世界 → 采集部署。
- 采集页分支：若识别到“已采集/已收获”，直接记录成功；否则识别“一键收获”并点击。
- 收获路径：识别奖励弹窗 → 点击关闭 → 验证回到画卷 → 记录成功。
- 收获后验证路径：若未识别奖励弹窗，则重新确认采集部署页，并识别“已采集/已收获”后记录成功。
- 边界：成功后关闭采集部署页和画卷页，验证回到主页；无法识别必要页面、奖励弹窗、采集完成状态或主页时记录失败。启动时还会处理已识别的残留商城边界。
- 当前任务暂无新增改动账目。

## 第 6 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/HERO_DISPATCH_DAILY.json`
- 入口：`MJA_HERO_DISPATCH_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/hero_dispatch_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `HERO_DISPATCH_DAILY_DEFINITION`
- 当前配置路径：主页 → 画卷 → 侠客派遣；识别派遣页后按“可领取 / 等待中 / 全部完成 / 进行中 / 无任务 / 可派遣”分支处理。
- 可派遣路径：选择首个可派遣任务 → 智能配置队伍 → 点击派遣；完成后以“全部完成 / 首个任务进行中 / 无派遣任务”之一作为成功后置条件。
- 可领取路径：选择首个可领取任务 → 点击领取 → 识别奖励弹窗 → 关闭奖励弹窗 → 回到派遣页继续处理。
- 边界：领取与派遣循环均有最多 6 次限制；成功或已完成状态会记录 `success`/`already_complete`，随后关闭派遣页和画卷页并验证主页；主页边界缺失时记录 `HERO_HOME_BOUNDARY_MISSING` 并失败。
- 当前任务暂无新增改动账目。

## 待实施改动

- `task_id`: `MAIL_REWARD_DAILY`
- `status`: `completed`
- 用户原始要求：删除第 5 步的空邮件判断；删除第 8 步的再次验证邮件为空；只要识别到奖励弹窗，就算任务成功。
- `clarification`: 用户已回答
- `user_explanation`: 并非等这个任务全部完成之后再记录成功；识别到奖励弹窗的当下就记录成功。

- `task_id`: `BUY_TEA_DAILY`
- `status`: `completed`
- 用户原始要求：购买条件简化为只要识别到“500文”就点击购买；购买结果弹窗出现即可记录成功；删除库存数量减少验证，即删除原第 13 步。
- `clarification`: 待用户回答唯一澄清问题
- `user_explanation`: 用户确认：是。只要识别到“500文”就点击购买，出现购买结果弹窗立即记录成功。

- `task_id`: `FREE_APPRAISAL_DAILY`
- `status`: `completed`
- 用户原始要求：第 5 步只要识别到鉴宝结果弹窗就算成功，不再识别数量、“鉴宝一次”和“鉴宝十次”；删除第 7 步的鉴宝页面已使用状态识别。
- `clarification`: 待用户回答唯一澄清问题
- `user_explanation`: 用户确认：识别到鉴宝结果弹窗立即记录成功，但仍关闭结果弹窗和鉴宝页面。

- `task_id`: `TRIAL_SWORD_DAILY`
- `status`: `completed`
- 用户原始要求：第 4 步不再识别当前收益等其他条件，直接点击领取按钮；删除第 9 步的免费次数已使用验证；识别到免费试剑奖励弹窗即可判定成功。
- `clarification`: 待用户回答唯一澄清问题
- `user_explanation`: 用户确认：识别到免费试剑奖励弹窗立即记录成功，但仍关闭奖励弹窗、关闭试剑页面并返回主页。

- `task_id`: `COLLECTION_DEPLOYMENT_DAILY`
- `status`: `completed`
- 用户原始要求：删除第 4 步的“已采集/已收获”判断；第 5 步只要点击“一键收获”就立即记录成功；删除第 6、7 步的奖励弹窗处理和收获状态验证。
- `clarification`: 已回答
- `user_explanation`: 用户确认：点击“一键收获”后立即记录成功，但仍关闭采集部署页、画卷页并返回主页。

- `task_id`: `WEEKLY_FREE_GIFT_MONDAY`
- `status`: `completed`
- 用户原始要求：第 4 步只要识别到奖励弹窗就立即记录成功；删除第 5 步的领取状态复验；删除第 6 步的付费礼包不适用分支；删除第 7 步的价格冲突/无法确认价格失败分支。
- `clarification`: 已回答
- `user_explanation`: 用户确认：记录成功后仍需关闭奖励弹窗、关闭商城页并返回主页。

- `task_id`: `SHADOW_RUINS_DAILY`
- `status`: `completed`
- 用户原始要求：删除第 2 步的普通副本/错误入口判断；进入画卷和蜃影入口后，若存在“探索中/可探索”就继续处理，最多 3 次，没有则停止继续探索；移除“暂无发现新的武墟/已击破”判断。进入探索前必须确认右下角“跳过战前准备”已勾选，未勾选则勾选。删除推荐阵容页面及跳过战前准备推荐相关处理；禁止点击传送按钮并删除卡住时的传送分支；删除主动识别/点击“开战”。战斗失败直接判定失败并退出到对应界面；不再识别“已击破”，只要领取最终奖励并退出即可成功。固定收尾为点击画卷、进入用户原话“肾虚”（当前配置目标为蜃影武墟）后，点击空白处，再点击叉号回到主页。
- `clarification`: 已回答
- `user_explanation`: 用户确认：每次识别一个“探索中/可探索”并点击进入或前往，计为 1 次，最多进行 3 次。

- `task_id`: `MARTIAL_STUDY_BREAKTHROUGH_DAILY`
- `status`: `completed`
- 用户原始要求：删除 Python 状态机中空槽位、研习、材料判断、突破确认、满槽判断等复杂状态机内容，不再执行这套复杂流程。
- `clarification`: 已回答
- `user_explanation`: 用户确认：删除后只保留当前 Pipeline 的简单流程，识别成功卡就领取并关闭奖励弹窗；没有成功卡就关闭研习页并记录成功。

- `task_id`: `EAT_STAMINA_FOOD_DAILY`
- `status`: `completed`
- 用户原始要求：删除食用后的数量减少验证；食用动作执行完成后即可记录成功。
- `clarification`: 已回答
- `user_explanation`: 用户确认：体力已满或者累计使用 5 次就记录成功；不再验证数量减少。

- `task_id`: `EQUIPMENT_DECOMPOSE_DAILY`
- `status`: `completed`
- 用户原始要求：品质筛选条件明确为“乙级及以下”。
- `clarification`: 已回答
- `user_explanation`: 用户确认：只修改为“乙级及以下”这一处，其他装备分解流程全部保持不变。

- `task_id`: `DUNGEON_SWEEP_DAILY`
- `status`: `completed`
- 用户原始要求：将目标副本从“燕王秘陵”改为“风雪神道”；风雪神道无需下滑即可识别。识别到扫荡结果弹窗即可记录成功，不再验证副本票耗尽。
- `clarification`: 已回答
- `user_explanation`: 用户确认：记录成功后仍需关闭结果弹窗、关闭副本页并返回主页。

- `task_id`: `JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY`
- `status`: `completed`
- 用户原始要求：第二档高价体力出现时不算失败，直接退出并记录成功；体力低于 20 时直接记录成功；删除“完成一次蜃影武墟挑战”的错误滚动识别；删除“领取”作为已完成状态的识别，因为该状态不存在。
- `clarification`: 已回答
- `user_explanation`: 用户确认：上述两类成功状态记录后，仍关闭相关弹窗、剑林页和日常页并返回主页。
- 补充要求：挑战按钮上方有“跳过战前准备”复选框；点击挑战前必须确认已勾选，未勾选则先勾选。勾选后点击挑战，跳过战斗准备页面直接进入战斗。

- `task_id`: `RING_CHALLENGE_DAILY`
- `status`: `completed`
- 用户原始要求：删除“论剑未开启”记为 `not_eligible` 的分支。
- `clarification`: 已回答
- `user_explanation`: 用户明确：直接删除“论剑未开启”判断，不设置替代结果；该标记说明进入了错误页面。

- `task_id`: `BREAK_ARRAY_MARTIAL_DAILY`
- `status`: `completed`
- 用户原始要求：破阵演武活动已经结束，第 17 个任务停止执行。
- `clarification`: 已回答
- `user_explanation`: 用户确认：保留第 17 个任务在目录中，但标记为停用，后续不再运行。

- `task_id`: `GUILD_ACTIVITY_CHALLENGE_DAILY`
- `status`: `completed`
- 用户原始要求：战斗胜利和战斗失败都认定为成功，因为无论是否击杀，最终造成的伤害都计入任务结果；不需要再验证剩余征讨次数是否为 `0/2`。
- `clarification`: 已回答
- `user_explanation`: 用户确认：战斗失败也判定成功后，仍需关闭结果弹窗、退出帮会活动、退出帮会、关闭功能面板并返回主页。

- `task_id`: `GUILD_AFFAIRS_DAILY`
- `status`: `completed`
- 用户原始要求：帮会事务页需要持续向下滚动，直到不能继续滚动为止；开始事务最多执行 6 次。
- `clarification`: 已回答
- `user_explanation`: 用户确认：滚动到底且没有更多可处理事务后，按“处理完可领取/可开始事务、没有付费项即成功”，再关闭事务页并返回主页。

## 第 16 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/RING_CHALLENGE_DAILY.json`
- 入口：`MJA_RING_CHALLENGE_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/ring_challenge_daily.json`
- 状态机定义：`agent/workflows/definitions/batch23.py` 中的 `RING_CHALLENGE_DAILY_DEFINITION`
- 进入路径：主页 → 功能面板 → 日常任务 → 擂台；如果日常页上有奖励弹窗，先关闭再进入擂台。
- 不适用与已完成：识别“论剑未开启”时记录 `not_eligible`；挑战次数已经为 0 时记录成功/已完成分支。当前 Pipeline 对次数耗尽主要记录 `success`，状态机部分分支使用 `already_complete`。
- 模式选择：进入擂台后，如果识别到“大师赛/大师赛模式”或积分达到 5000，则进入扫荡分支；否则进入手动挑战分支。扫荡要求同时识别扫荡按钮、擂台券和票数。
- 扫荡路径：点击“扫荡”→确认消耗擂台券→识别扫荡结果→关闭结果弹窗→关闭擂台页→记录 `success`。
- 手动路径：识别挑战次数和擂台券后，点击匹配/挑战；处理匹配加载、战前准备、开始战斗、跳过战斗和战斗结果。战斗胜利或失败只要结果可识别都会关闭结果并继续下一次；结果无法识别则记录 `RING_BATTLE_RESULT_UNKNOWN` 失败。最多执行 12 次，次数耗尽后记录成功。
- 收尾：正常成功分支会关闭对手页和擂台页；部分“未开启”或次数耗尽分支直接进入公共停止节点，未必经过返回主页验证。
- 安全边界：任务标记为消耗型和战斗型，最多 120 步；擂台券资源上限为 12；匹配、战斗、跳过和结果关闭最多各 12 次，扫荡最多 1 次。
- 已知风险：Pipeline 和状态机对“次数耗尽”使用的成功/已完成状态不完全一致；同时部分终止分支缺少统一的页面收尾。状态机在手动模式下要求可识别剩余次数，无法确认时会 fail closed，避免重复消耗擂台券。
- 当前任务暂无新增改动账目。

## 第 17 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/BREAK_ARRAY_MARTIAL_DAILY.json`
- 入口：`MJA_BREAK_ARRAY_MARTIAL_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/break_array_martial_daily.json`
- 状态机定义：`agent/workflows/definitions/break_array_martial_daily.py` 中的 `BREAK_ARRAY_MARTIAL_DAILY_DEFINITION`
- 进入路径：主页 → 活动入口 → 活动页 →“破阵演武”；如果已经停留在活动页或破阵页，状态机允许恢复。
- 初始判断：识别活动未开放时记为 `not_eligible`；识别“今日已完成/挑战已完成/剩余次数为 0”时记为 `already_complete`；识别开始按钮后进入挑战循环。
- 挑战流程：最多点击开始挑战 3 次。每次都要识别确认弹窗中“消耗 1 次挑战次数”和进入准备界面的提示，点击确认；准备页还需识别阵容、首领战斗、战斗时长 02:00、战术谱和开始按钮。
- 战斗与结果：等待加载或战斗状态，最多轮询 12 次；识别胜利结果后关闭结果弹窗。结果页最多轮询 3 次；识别失败或无法识别结果都会失败，不会把失败结果当成成功继续。
- 成功边界：结果关闭后重新识别破阵页；完成标记或剩余次数为 0 时记录 `success`，三次挑战完成是 Pipeline 的成功后置条件。状态机还要求挑战、确认、开始战斗次数一一匹配。
- 安全边界：任务标记为战斗型，最多 64 步；开始挑战、确认、开始战斗最多各 3 次，战斗等待最多 12 次，结果等待最多 3 次；付费、验证或未知弹窗会 fail closed。
- 已知风险：任务标签是“每日三次”，但当前 OCR 次数识别使用 `x/9`；成功、已完成和不适用分支都直接进入公共停止，没有统一关闭活动页、破阵页或验证主页的收尾节点。
- 当前任务暂无新增改动账目。

## 第 18 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/GUILD_ACTIVITY_CHALLENGE_DAILY.json`
- 入口：`MJA_GUILD_ACTIVITY_CHALLENGE_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/guild_activity_challenge_daily.json`
- 状态机定义：未发现该任务对应的独立 Python 状态机定义；当前由 Pipeline 和 `agent/custom/support/policy.py` 的任务策略驱动。
- 进入与恢复：从主页 → 功能面板 → 帮会 → 帮会活动 →“幻境征讨”。启动时会先检查验证码/安全验证和付费页面；还会尝试清理副本、剑林、日常任务等残留页面，必要时重新启动游戏。
- 活动页判断：需要同时识别“幻境征讨”和“讨伐中/今日剩余征讨次数”等上下文。剩余次数为 `0/2` 时走已完成分支，并依次退出活动页、帮会页、功能面板，确认回到主页后记录 `already_complete`。
- 挑战流程：剩余次数为 `1/2` 或 `2/2` 且识别到“挑战/开始挑战”时进入挑战循环，最多挑战 2 次；若出现确认页，点击“确认”；若出现战前准备页，点击开始按钮进入战斗。
- 战斗判定：通过战斗计时器、首领和自动战斗控件识别战斗进行中，最长等待 180 秒。识别到“战斗胜利”后点击空白处关闭结果弹窗，随后当前主分支立即记录 `success`；识别到“战斗失败”、未知结果、验证码或付费页面则记录失败并中止。
- 当前成功语义的缺口：成功结果关闭后直接进入公共停止节点，没有经过文件中另行定义的退出活动页、退出帮会页、关闭功能面板和主页确认节点；同时成功结果声明的后置条件是 `guild.remaining_conquest_0_of_2`，但主成功路径没有实际复验 `0/2`，用于复验剩余次数的后置节点目前未接入这条主路径。
- 策略限制：任务标记为有状态/战斗型，最多 32 步；挑战、确认、开始战斗、结果关闭各最多 2 次，失败结果关闭最多 1 次；付费和安全验证均 fail closed。
- 当前任务暂无新增改动账目。

## 第 19 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/GUILD_AFFAIRS_DAILY.json`
- 入口：`MJA_GUILD_AFFAIRS_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/guild_affairs_daily.json`
- 状态机定义：未发现该任务对应的独立 Python 状态机定义；当前由 Pipeline 和 `agent/custom/support/policy.py` 的任务策略驱动。
- 进入路径：主页 → 功能面板 → 帮会 → 帮会事务。若已停留在帮会事务页、帮会页、功能面板或主页，Pipeline 会从对应位置恢复；无法恢复时会尝试重新启动游戏。
- 事务识别：按页面上的事务行逐项处理。可领取时识别“领取奖励”并点击，随后识别奖励弹窗并关闭；可开始时识别“开始事务”并点击，随后确认该行进入“进行中/事务中/已完成/已领取/暂无事务/无可领取”等无操作状态。
- 行处理范围：当前 Pipeline 定义了首行和后续 3 个行位，领取后会重新检查首行，因为领取后列表可能向上收拢；最多领取 6 次、开始事务 4 次。
- 成功边界：所有已定义行都进入“已处理或无操作”状态后记录 `success`，然后关闭帮会事务页并确认回到帮会页；当前主成功路径随后直接进入公共停止节点，没有显式返回主页。
- 付费保护：任一事务行出现“购买、付费、充值、价格、元、消耗”等付费标记，立即记录 `GUILD_FIRST_ROW_PAID_OR_AMBIGUOUS` 失败，不会尝试购买。
- 策略限制：任务标记为受保护领取/有状态型，最多 24 步；打开功能面板、帮会和帮会事务各 1 次，领取奖励最多 6 次，开始事务最多 4 次，关闭事务页 1 次。
- 已知风险：策略注释按最多 6 行描述，但当前 Pipeline 只有首行加后续 3 个行位，没有看到滚动或第 5、6 行处理；此外成功后只确认帮会页，没有完成返回主页的后置验证。
- 当前任务暂无新增改动账目。

## 第 20 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/GUILD_DONATION_DAILY.json`
- 入口：`MJA_GUILD_DONATION_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/guild_donation_daily.json`
- 状态机定义：`install/mfw-shared-convergence-20260813-r3/agent/workflows/definitions/guild_donation_daily.py` 中的 `GUILD_DONATION_DAILY_DEFINITION`；同时有 `agent/custom/support/policy.py` 的任务策略。
- 进入路径：主页 → 功能面板 → 帮会 → 捐献。Pipeline 同时保留普通捐献页面和另一套 Android/兼容页面的进入、识别与收尾路径。
- 已完成判断：进入捐献页后识别捐献次数为 `9/10`，记录 `already_complete`，然后关闭捐献页、帮会页、功能面板并确认回到主页。
- 执行条件：只有同时识别剩余次数 `10/10` 和“免费捐献”按钮时才点击一次免费捐献；普通页面与兼容页面都以捐献后新鲜识别到 `9/10` 作为完成依据。兼容页面还会识别并关闭奖励弹窗。
- 成功收尾：捐献成功后关闭捐献页、帮会页、功能面板，确认主页后记录 `success`；无法确认 `9/10`、次数格式异常、未加入帮会/未开放、付费页面、验证码或未知弹窗时记录不适用或失败。
- Python 状态机语义：把捐献次数作为业务不变量，而不是点击次数；只允许在当前画面证明 `10/10` 时点击，并要求后续画面证明 `9/10`，最多执行 1 次免费捐献。
- 策略限制：任务标记为有状态型，最多 24 步；进入功能面板、帮会和捐献页各 1 次，免费捐献最多 1 次，关闭捐献页、帮会页和功能面板各 1 次。
- 已知风险：普通页面的主 Pipeline 主要依赖捐献后 `9/10` 复验，未单独定义普通页面奖励弹窗关闭节点；此外普通页面、兼容页面和 Python 状态机的页面识别证据不完全相同，后续改动时需要统一成功口径。
- 当前任务暂无新增改动账目。

## 第 21 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/DAILY_TASK_REWARD_CLAIM_DAILY.json`
- 入口：`MJA_DAILY_TASK_REWARD_CLAIM_DAILY_START`
- Pipeline：`install/mfw-shared-convergence-20260813-r3/resource/base/pipeline/daily/daily_task_reward_claim_daily.json`
- 状态机定义：`install/mfw-shared-convergence-20260813-r3/agent/workflows/definitions/batch1.py` 中的 `DAILY_TASK_REWARD_CLAIM_DAILY_DEFINITION`，并由 `daily_task_reward_claim_daily.py` 导出；同时有 `agent/custom/support/policy.py` 的任务策略。
- 进入路径：主页 → 功能面板 → 日常任务。若已经停留在日常页或奖励弹窗，Pipeline 支持直接恢复；否则打开功能面板并进入日常任务页。
- 领取对象：识别日常任务行上的“领取”按钮后点击，识别“恭喜获得/获得”奖励弹窗并关闭；另独立识别已解锁的活跃度宝箱并领取、关闭奖励弹窗。
- 扫描机制：日常任务页识别不到当前可领取项时，会在任务区域向下滑动，最多 5 次；初始扫描或领取后扫描到“暂无可领取/前往”或已领取状态时，记录 `already_complete` 或 `success`。
- 成功与收尾：领取后继续扫描，确认没有可领取奖励后记录 `success`；一开始就没有可领取项则记录 `already_complete`。随后关闭日常页、关闭功能面板并确认回到主页。
- 失败边界：滑动次数耗尽但无法确认“没有可领取”，或奖励弹窗、日常页、关闭按钮等必要证据缺失时记录失败；单次最多领取日常任务行 50 次、活跃度宝箱 10 次、关闭奖励弹窗 60 次。
- Python 状态机差异：其当前决策逻辑在首次领取任务行或宝箱并关闭弹窗后即可完成，不一定继续领取同一页上的其他奖励；Pipeline 则设计为领取后继续扫描直到无可领取，二者成功范围不完全一致。
- 策略限制：任务标记为受保护领取型，最多 70 步；功能面板、日常页和收尾动作各有 1 次上限，滚动最多 5 次。
- 当前任务暂无新增改动账目。

## 第 22 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/BATTLE_PASS_REWARD_DAILY.json`
- 入口：`MJA_BATTLE_PASS_REWARD_DAILY_START`
- Pipeline：`install/mfw-shared-convergence-20260813-r3/resource/base/pipeline/daily/battle_pass_reward_daily.json`
- 状态机定义：`install/mfw-shared-convergence-20260813-r3/agent/workflows/definitions/batch1.py` 中的 `BATTLE_PASS_REWARD_DAILY_DEFINITION`，并由 `battle_pass_reward_daily.py` 导出；同时有 `agent/custom/support/policy.py` 的任务策略。
- 进入路径：主页 → 战令。若已经停留在战令任务页、奖励页、奖励弹窗或物品弹窗，Pipeline 和状态机都支持恢复；否则打开战令并进入任务页。
- 任务奖励：在任务页识别“领取”后点击，处理奖励弹窗或物品弹窗并关闭；继续检查任务页，直到没有可领取任务，再切换到“奖励”页。
- 基础奖励：在奖励页识别“基础版”和战令等级；出现红点奖励时点击领取，处理并关闭奖励/物品弹窗，继续检查。识别“已领取”文字或已领取模板时，判定基础奖励已完成。
- 成功与已完成：原本就没有任务奖励且基础奖励已领取时记录 `already_complete`；完成领取后确认基础奖励已领取，关闭战令页并确认回到主页，再记录 `success`。
- 失败边界：任务页或奖励页无法确认、奖励/物品弹窗无法关闭、基础奖励既没有可领取红点也没有明确已领取证据、或无法返回主页时失败。任务奖励和基础奖励各最多领取 50 次，奖励弹窗最多关闭 50 次。
- 当前没有独立滚动节点：任务奖励领取区域和基础奖励红点都使用固定识别区域，Pipeline 未定义向下滚动步骤；是否能覆盖多行/多等级奖励依赖领取后页面重新布局。
- Python 状态机差异：任务奖励目标消失后直接切换奖励页；奖励页没有红点且没有“已领取”证据会失败，不会把单纯进入奖励页当作成功。Pipeline 额外区分初始无任务奖励和领取后无任务奖励。
- 策略限制：任务标记为受保护领取型，最多 65 步；战令入口、任务页、奖励页各最多打开 1 次，任务奖励和基础奖励最多各领取 50 次，关闭战令页 1 次。
- 当前任务暂无新增改动账目。

## 第 15 个任务说明依据

- 任务配置：`install/mfw-shared-convergence-20260813-r3/tasks/日常/JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY.json`
- 入口：`MJA_JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY_START`
- Pipeline：`resource/base/pipeline/daily/jianlin_resource_condensate_stamina_daily.json`
- 状态机定义：`agent/workflows/definitions/jianlin_resource_condensate_stamina_daily.py` 中的 `JIANLIN_RESOURCE_CONDENSATE_STAMINA_DAILY_DEFINITION`
- 进入路径：主页 → 功能面板 → 日常/每日任务 →“战胜一次剑林的首领”→ 剑林；日常列表最多向上滚动 3 次寻找目标行。启动时还会清理帮会、擂台、副本、蜃影武墟、战斗结果等残留页面。
- 任务已完成判断：同一日常行同时识别目标文字和右侧“领取/已完成/已领取”之一时，记录 `already_complete`。
- 资源路径：进入剑林后选择“凝晶”资源，并确认挑战按钮、当前体力和挑战条件均可识别。
- 体力购买：若当前体力不足，打开补充体力页面；只在同一画面确认“+80”、价格 10、紫色魂玉和购买按钮后购买 1 次，再确认购买弹窗并关闭结果。购买后若出现更高价的第二档体力提示，记录 `JIANLIN_ESCALATED_STAMINA_OFFER` 失败；可识别的安全关闭提示则关闭后继续。
- 挑战计划：读取当前体力、单次消耗、可见最大挑战数量和安全倍率（1/2/3），计算不超过体力和数量上限的组合；先设置挑战次数和倍率，再点击挑战。最多执行 12 个挑战循环，无法证明安全组合或体力不足时记为 `not_eligible` 或失败。
- 战斗与成功：点击挑战后进入战斗，按识别到的次数/倍率组合启动战斗；识别战斗结果并关闭结果弹窗后，当前 Pipeline 直接记录 `success`，随后关闭剑林页、日常页并确认主页。
- 其他结束状态：日常行已完成为 `already_complete`；体力不足且无法挑战为 `not_eligible`；页面、购买证据、数量/倍率或战斗结果无法确认时记录失败。
- 安全边界：任务标记为消耗型和战斗型，最多 160 步；最多消耗 1 个紫色魂玉和 360 点体力；购买最多 1 次，挑战、战斗和结果关闭各最多 12 次。
- 已知风险：日常滚动识别 `jianlin.daily.scroll` 当前期望的是“完成一次蜃影武墟挑战”，与目标行“战胜一次剑林的首领”不一致；此外“领取”被纳入已完成标记，可能把可领取状态与已完成状态混在一起。状态机有日常完成复核逻辑，但当前 Pipeline 的战斗成功分支主要以结果弹窗关闭作为成功证据。
- 当前任务暂无新增改动账目。
