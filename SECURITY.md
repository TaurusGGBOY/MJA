# Security policy

Do not report credentials, account identifiers, private screenshots, or other
sensitive data in a public issue. Remove the data from local logs and fixtures
and contact the maintainers through a private channel before opening a report.

This project does not accept credentials or secrets in configuration files.
Use environment variables or ignored local configuration for development.
